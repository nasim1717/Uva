#include<bits/stdc++.h>
using namespace std;
main(){
    int t;
    string s;
    map<string,int>mp;
    cin>>t;
    while(t--){
        string st;
        cin>>s;
        mp[s]++;
        getline(cin,st);

    }
    for(auto it: mp){
        cout<<it.first<<" "<<it.second<<endl;
    }
return 0;
}
